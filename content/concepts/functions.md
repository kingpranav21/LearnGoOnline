# Functions + return values

## What you learned
- Function params look like: `name type`.
- The return type goes after the parameter list.

## Example

```go
func add(a int, b int) int {
  return a + b
}
```

## Try this
- Write `mul(a, b int) int`.
- Return two values (Go supports multiple returns).


