# `for` loops

## What you learned
- Go has a single looping keyword: `for`.
- The classic form is: `for init; condition; post { ... }`.

## Example

```go
for i := 0; i < 3; i++ {
  fmt.Println(i)
}
```

## Try this
- Change the loop to print `0..9`.
- Print only even numbers (hint: `%`).


